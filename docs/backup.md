Analise as documentações principais do projeto de TCC atualmente temos como servidor rtmp o nginx

a minha ideia é suprimir a questão do transcode já que não funciona de forma nenhuma e usarmos da seguinte forma

vamos ter o nginx e o srs e quero tentar usar o rtmp e o webrtc assim vou ter aquela questa de 2² sao dois servidores e dois procotoles e para o hls ou transcode use o padrão do servidor 

então inicialmente atualize os documentos e diagrams do projeto e logo em seguida analise o projeto e crie um todo para finalizarmos o projeto com esse novo direcionamento
