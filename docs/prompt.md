leia o utils.md antes de realizar as tarefas.

atualize esse arquivo quando finalizar

tarefas:

[x] crie uma feature para listar as streams com o status live além disso atualize de tempos em tempo pode ser usando o react query tambem no frontend